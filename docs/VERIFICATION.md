# WeCom 插件验收清单（v0.5.6）

> 一条命令更新 → 按顺序过一遍 → 任何一步不符，把企微截图 + 终端日志（含 `[wecom-plus]` 行）发回。

## 0. 更新与启动

```powershell
cd D:\deepseek\ds-harness-wecom
git push origin main

cd D:\deepseek\deepseek-harness
pnpm dsh plugin --profile web remove deepseek-harness-wecom-plus
pnpm dsh plugin --profile web add github:fryghost/deepseek-harness-wecom-plus
pnpm dsh web
```

- 验收点 A：DSH 正常打开（网页 3080 可访问，无 fatal 报错）；
- 验收点 B：打开 设置 → WeCom 企微，右上角状态为「已连接（WeCom AI Bot authenticated）」。

## 1. 基础连通

| # | 操作 | 预期 |
| --- | --- | --- |
| 1.1 | 企微发 `/bot-ping` | 回复 `pong — …已连接` |
| 1.2 | 发 `/bot-status` | 状态说明 |
| 1.3 | 发 `/new` | 提示已开新对话 |

## 2. 流式输出（核心体验）

| # | 操作 | 预期 |
| --- | --- | --- |
| 2.1 | 发「写一篇带标题、列表、代码块的简短说明」 | ① 立即出现「正在思考…」② 文字**逐段流式出现** ③ 最终定格完整 Markdown |
| 2.2 | 发「列出我当前目录的文件」 | 流中出现「正在执行工具 `…`」字样，随后给结果 |
| 2.3 | 发一条会在中途调用工具的复杂任务 | 文本不重复（重试/多步不出现重复段落） |

## 3. 提问卡片（ask_user_question）

| # | 操作 | 预期 |
| --- | --- | --- |
| 3.1 | 发「用 ask_user_question 问我一个要不要继续的问题」 | **只收到一张确认卡**（无前置大段说明） |
| 3.2 | 点「继续」按钮 | ① 卡片原位仍是按钮卡：选项保留、选中项打 ✓、辅助文案「已选择「继续」，正在处理…」② **无灰色"已收到"气泡** ③ 模型按选择继续回复 |
| 3.3 | 发「问我一个带说明的选择题，两个选项各自说明利弊」 | Markdown 说明（编号+说明）+ 按钮卡 |
| 3.4 | 发「问我一个选项文字很长的选择题」 | **不出现被截断的按钮**；改为 Markdown 编号列表 + 文字卡 |
| 3.5 | 对 3.4 回复 `2` | 立即回执「已收到你的回答，正在处理…」→ 模型按第 2 项继续 |
| 3.6 | 发「一次问我两个问题」 | 两题**依次**出现（答完第一题出第二题） |

## 4. 模板卡片（wecom_send_card）

| # | 操作 | 预期 |
| --- | --- | --- |
| 4.1 | 发 `/bot-card-test` | 按钮卡 → 点击 → 卡片原位仍是按钮卡（选项保留、选中打 ✓、辅助文案「已选择「确认收到」，正在处理…」）→ 模型回复；再点任意按钮无反应 |
| 4.2 | 发「用 wecom_send_card 发一个下拉卡片，两个下拉框」 | multiple_interaction 卡；提交后模型能读出**每个下拉框选中的值** |
| 4.3 | 发「用 wecom_send_card 发一个投票卡」 | vote_interaction 卡；提交后模型读出勾选项 |

## 5. 通道隔离（网页不回归）

| # | 操作 | 预期 |
| --- | --- | --- |
| 5.1 | 在**网页任意会话**让模型问一个选择题 | 网页提问面板照常弹出（不受插件影响） |
| 5.2 | 网页打开同一个企微会话继续提问 | 提问走**网页面板**（非企微卡片） |

## 6. 媒体链路

| # | 操作 | 预期 |
| --- | --- | --- |
| 6.1 | 企微发一张图片 | 模型能描述图片（视觉模型）；文本模型则收到附件说明且不报错 |
| 6.2 | 企微发一个小文件 | 模型能读取内容总结 |
| 6.3 | 让模型「把 README.md 作为文件发给我」 | 出现 `wecom_send_file` 调用，企微收到附件 |

## 7. 健壮性

| # | 操作 | 预期 |
| --- | --- | --- |
| 7.1 | 提问后 5 分钟内不答 | 5 分钟后模型收到超时错误并以正常消息解释，通道不挂死 |
| 7.2 | 提问中点 `/bot-cancel` | 提问中止，回合结束 |
| 7.3 | 设置页保存（改卡片模式） | 页面提示保存成功，通道**热重连**，无需重启 |
| 7.4 | 重启 DSH | 企微会话历史保留（继续上一话题上下文） |

## 8. 终端日志速查

每条收到的企微消息必有一行（图片丢失时先看这行，能区分「企微没发图」还是「解析丢了图」）：

```
[wecom-plus] inbound msgid=... type=mixed chat=single from=... mixed items=4(text=1,image=3,bare=0)
```

- `mixed items=N(image=M,bare=B)`：消息里带了 N 个混排项，其中 M 个图片项有下载载荷；`bare>0` 表示企微发了图片位但没给载荷（会话里会出现「no downloadable payload」说明文字）；
- 纯文本消息只有 `text=长度ch`；若连 `inbound` 行都没出现，消息根本没到达插件（检查 `allowed` 策略与消息去重）。

每次卡片点击必有一行：

```
[wecom-plus] card click msgid=... task=... key=... questionLabel=... answered=... raw={...}
```

- `questionLabel=<选项名> answered=true` → 点击命中提问，正常；
- `task=/key=` 有值但 `answered=false` → 旧卡/非提问卡点击（按普通点击处理）；
- `task=/key=` 为空 → 发我（平台载荷形状变化）；
- 出现 `WeCom stream finish failed, falling back to proactive send` → 流式通道故障，已自动降级（用户仍会收到回复）。

## 9. 一键回归（快速版）

按序发送：`/bot-ping` → `/bot-card-test`（点按钮）→ 「给我三个不超过四个字的选项让我选」（点按钮）→ 「问我一个选项很长的选择题」（回数字）→ 「用 wecom_send_card 发一个两个下拉框的卡片」（提交）→ 图片 → 文件。

## 10. WeCom CLI 体检（v0.9.0）

| # | 操作 | 预期 |
| --- | --- | --- |
| 10.1 | 未安装 CLI 时打开设置页 CLI 集成卡片 | 显示「未安装」+ 一键安装按钮 |
| 10.2 | 点击一键安装 | 实时回显输出尾部，完成后状态变为「已安装 · 待授权」 |
| 10.3 | 点击发起授权 | 卡片内出现二维码，手机企微扫码后 2 秒内变「已授权」 |
| 10.4 | 企微内发送 /bot-cli | 按状态返回对应引导文案；已授权时显示版本与就绪信息 |
| 10.5 | 授权中点击取消 | 二维码消失，状态回到「待授权」 |
